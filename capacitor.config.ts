import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wer-hat-die-maschiene-gemacht-frontend',
  webDir: 'www'
};

export default config;
